[package]
name = "ferrishost-core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[package.metadata.dist]
dist = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
