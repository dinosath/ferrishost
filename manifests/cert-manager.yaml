apiVersion: v1
kind: Namespace
metadata:
  name: cert-manager

---
# Placeholder for cert-manager manifests
# In a real implementation, these would be fetched from upstream
# or vendored as compressed resources

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: certificates.cert-manager.io
spec:
  group: cert-manager.io
  names:
    kind: Certificate
    plural: certificates
  scope: Namespaced

---
apiVersion: v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
